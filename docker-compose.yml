version: "3.8"

networks:
  kittygram_network: # Создаём общую сеть

services:
  postgres:
    image: postgres:13
    container_name: postgres
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - kittygram_network # Добавляем контейнер в сеть

  kittygram_backend:
    build:
      context: ./backend
    container_name: backend
    env_file: .env
    depends_on:
      - postgres
    volumes:
      - static:/code/static
      - media:/code/media
    ports:
      - "8000:8000"
    networks:
      - kittygram_network # Добавляем контейнер в сеть

  kittygram_frontend:
    build:
      context: ./frontend
    container_name: frontend
    volumes:
      - static:/app/build
    ports:
      - "3000:8000"
    networks:
      - kittygram_network # Добавляем контейнер в сеть

  kittygram_gateway:
    image: nginx:latest
    container_name: gateway
    volumes:
      - static:/var/www/static
      - media:/var/www/media
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
    depends_on:
      - kittygram_backend
      - kittygram_frontend
    networks:
      - kittygram_network # Добавляем контейнер в сеть

volumes:
  pg_data:
  static:
  media:
